// Colocamos la ruta donde va estar el servidor
const server = 'http://localhost:7071';
// Colocamos donde va estar uvicado el api o las rutas para hacer el crud.
// create , delete, update, read
const apiPath = '/api';

// Exportamos la url para la api
export const apiUrl = `${server}${apiPath}`;
